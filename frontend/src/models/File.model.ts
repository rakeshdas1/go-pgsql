export interface FileModel {
    fileId: number;
    fileName: string;
    uploadedSize: string;
    percentage: string;
    eta: string;
    fileSize: string;
    transferSpeed: string;
    taskId: number;
}